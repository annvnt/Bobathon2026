# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Data validation / settings ---
pydantic==2.10.4
pydantic[email]==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Relational DB (SQLAlchemy; Postgres or SQLite) ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10   # Postgres driver (optional; SQLite works out of the box)

# --- Vector DB ---
chromadb==0.5.23

# --- Background jobs ---
APScheduler==3.11.0

# --- GenAI / LLM (abstracted; mock fallback works with none of these) ---
langchain==0.3.14
langchain-core==0.3.29
langchain-ibm==0.3.5      # IBM watsonx integration (optional)
httpx==0.28.1

# --- Alerts (optional; mock fallback if no creds) ---
twilio==9.4.1
